<Window
    x:Class="SurfComm.Connect.Views.ParameterSelectionWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Select Parameters"
    Width="700"
    Height="500"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner">

    <Grid Margin="20">

        <DataGrid
            Margin="0,0,0,50"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            HeadersVisibility="Column"
            IsReadOnly="False"
            ItemsSource="{Binding Parameters}">

            <DataGrid.Columns>

                <!--  Checkbox  -->
                <DataGridCheckBoxColumn
                    Width="60"
                    Binding="{Binding IsSelected, Mode=TwoWay}"
                    Header="Select" />

                <!--  Parameter Name  -->
                <DataGridTextColumn
                    Width="150"
                    Binding="{Binding Name}"
                    Header="Parameter"
                    IsReadOnly="True" />

                <!--  ID  -->
                <DataGridTextColumn
                    Width="60"
                    Binding="{Binding Id}"
                    Header="ID"
                    IsReadOnly="True" />

                <!--  Min  -->
                <DataGridTextColumn
                    Width="100"
                    Binding="{Binding Min, Mode=TwoWay}"
                    Header="Min" />

                <!--  Max  -->
                <DataGridTextColumn
                    Width="100"
                    Binding="{Binding Max, Mode=TwoWay}"
                    Header="Max" />

            </DataGrid.Columns>
        </DataGrid>

        <StackPanel
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Orientation="Horizontal">

            <Button
                Width="100"
                Margin="10,0"
                Command="{Binding CancelCommand}"
                Content="Cancel" />

            <Button
                Width="100"
                Margin="10,0"
                Command="{Binding ConfirmCommand}"
                Content="OK" />
        </StackPanel>

    </Grid>
</Window>
