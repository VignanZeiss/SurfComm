<UserControl
    x:Class="SurfComm.Connect.Views.CommanderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SurfComm.Connect.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Width="100"
                Margin="10,0"
                FontSize="12"
                Text="Enter Commad : " />
            <TextBox
                Width="150"
                Margin="100,0,10,0"
                Text="{Binding Command, Mode=TwoWay}" />
            <Button Command="{Binding Execute}" Content="Execute" />

        </StackPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">

            <TextBlock Margin="10" Text="{Binding Response}" />
        </ScrollViewer>
    </Grid>

</UserControl>
