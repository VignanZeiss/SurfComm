<Window x:Class="SurfComm.Connect.Views.ParameterPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SurfComm.Connect.Views"
        mc:Ignorable="d"
        Title="Add Parameter"
        SizeToContent="WidthAndHeight">
    <StackPanel Margin="20">
        <TextBlock Text="Select Parameter" FontWeight="Bold"/>

        <ComboBox ItemsSource="{Binding AvailableParameters}"
                  SelectedItem="{Binding SelectedParameter}"
                  DisplayMemberPath="Name"
                  Width="200"
                  Margin="0 10 0 20"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="0 0 10 0"/>
            <Button Content="Add" Command="{Binding SaveCommand}"/>
        </StackPanel>
    </StackPanel>
</Window>
